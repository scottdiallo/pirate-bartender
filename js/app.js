var drinkTypeHandler = function(){
    // Will get values from the form
    // to determine how the pirate makes the drink
};
var Drink = function(isStrong, isSalty, isBitter, isSweet, isFruity){
    this.isStrong = isStrong;
    this.isSalty = isSalty;
    this.isBitter = isBitter;
    this.isSweet = isSweet;
    this.isFruity = isFruity;

};

// Note the new
var Scott = new Person("Scott", 24, "San Fransico");


$(document).ready(function(){

});
